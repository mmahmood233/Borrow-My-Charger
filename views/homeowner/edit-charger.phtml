<?php include 'views/includes/header.phtml'; ?>

<div class="container mt-4">
    <h1>Edit Charge Point</h1>
    <p>Update the details of your charge point.</p>
    
    <?php if (!empty($view->error)): ?>
        <div class="alert alert-danger"><?php echo $view->error; ?></div>
    <?php endif; ?>
    
    <?php if (!empty($view->success)): ?>
        <div class="alert alert-success"><?php echo $view->success; ?></div>
    <?php endif; ?>
    
    <form action="edit-chargepoint.php?id=<?php echo $view->chargePoint['id']; ?>" method="POST" enctype="multipart/form-data">
        <div class="form-group">
            <label for="address">Address:</label>
            <input type="text" id="address" name="address" class="form-control" value="<?php echo htmlspecialchars($view->chargePoint['address']); ?>" required>
        </div>
        
        <div class="row">
            <div class="col-md-6">
                <div class="form-group">
                    <label for="latitude">Latitude:</label>
                    <input type="text" id="latitude" name="latitude" class="form-control" value="<?php echo $view->chargePoint['latitude']; ?>" required>
                </div>
            </div>
            <div class="col-md-6">
                <div class="form-group">
                    <label for="longitude">Longitude:</label>
                    <input type="text" id="longitude" name="longitude" class="form-control" value="<?php echo $view->chargePoint['longitude']; ?>" required>
                </div>
            </div>
        </div>
        
        <div class="form-group">
            <label for="price">Price per kWh (£):</label>
            <input type="number" id="price" name="price" step="0.01" min="0.01" class="form-control" value="<?php echo $view->chargePoint['price']; ?>" required>
        </div>
        
        <div class="form-check mb-3">
            <input type="checkbox" id="availability" name="availability" class="form-check-input" <?php echo $view->chargePoint['availability'] ? 'checked' : ''; ?>>
            <label class="form-check-label" for="availability">Available for booking</label>
        </div>
        
        <div class="form-group">
            <label for="image">Upload New Image (optional):</label>
            <input type="file" id="image" name="image" class="form-control-file">
        </div>
        
        <?php if ($view->chargePoint['image']): ?>
            <div class="mb-3">
                <p>Current Image:</p>
                <img src="uploads/<?php echo $view->chargePoint['image']; ?>" class="img-thumbnail" style="max-width: 200px;" alt="Current Charge Point Image">
            </div>
        <?php endif; ?>
        
        <button type="submit" name="edit_chargepoint" class="btn btn-primary">Update Charge Point</button>
        <a href="homeowner-dashboard.php" class="btn btn-secondary">Cancel</a>
    </form>
</div>

<?php include 'views/includes/footer.phtml'; ?>